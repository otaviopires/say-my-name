name: A workflow for my Say My Name Action
on: push

job:
        build:
                name: Say My Name Action
                runs-on: ubuntu-latest
                steps:
                        - uses: actions/checkou@v1
                        - uses: ./say-my-name
                          with:
                                  MY_NAME: "Otavio"
